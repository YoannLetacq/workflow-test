# Brief — mdsite (static site generator)
A small CLI that converts a folder of Markdown files (content/*.md) into an HTML site
(site/*.html + site/index.html) using a theme template. Delivered = working build +
green tests + docs, committed to main. Poles: engine (build+tests), theme (template+css),
docs (README).
